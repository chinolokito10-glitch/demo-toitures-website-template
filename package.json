{
  "name": "toiture-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && serve dist --listen 3000",
    "start": "serve dist --listen 3000",
    "build": "node scripts/build.js",
    "test": "npm run build && node --test tests/*.test.js",
    "test:browser": "npm run build && node tests/browser.js",
    "start:leads": "node scripts/lead-server.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "playwright": "^1.51.1",
    "prettier": "^3.9.6",
    "serve": "^14.2.6",
    "serve-handler": "^6.1.7",
    "sharp": "^0.35.4"
  }
}
